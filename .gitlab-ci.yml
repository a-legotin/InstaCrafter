image: mcr.microsoft.com/dotnet/sdk:6.0-focal

stages:
  - build

build:
  stage: build
  script:
    - dotnet restore InstaCrafter/src/common/InstaCrafert.Extensions/InstaCrafert.Extensions.csproj --locked-mode
    - dotnet build InstaCrafter/src/common/InstaCrafert.Extensions/InstaCrafert.Extensions.csproj -c Release --no-restore --no-dependencies